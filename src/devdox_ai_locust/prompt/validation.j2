You are enhancing utility classes for Locust load testing validation.

{% if constraints %}
{{ constraints }}
{% endif %}

**EXISTING UTILITY CLASSES** (for reference only - DO NOT reproduce):
```python
{{base_content}}
```

**Validation Patterns Needed**:
{{ validation_patterns }}

---

## YOUR TASK: Generate ONLY NEW METHODS

Return **ONLY** the new methods to ADD to the utility classes. Do NOT return full classes.

### CRITICAL OUTPUT RULES:

1. **OUTPUT FORMAT**: Wrap your response in `<new_methods>` tags with class context:
   ```
   <new_methods>
   # For ResponseValidator:
   def validate_user_response(self, response):
       """Validate user API response structure."""
       ...

   # For RequestLogger:
   def log_api_timing(self, endpoint, duration):
       """Log API call timing for analysis."""
       ...
   </new_methods>
   ```

2. **ONLY INCLUDE**:
   - New method definitions (def method_name(self, ...):)
   - A comment indicating which class the method belongs to
   - Docstrings for each method
   - Method body with implementation

3. **DO NOT INCLUDE**:
   ❌ Class definitions (no `class ResponseValidator:`)
   ❌ Import statements (no `import` or `from`)
   ❌ Existing methods that are already in the classes
   ❌ Any code that already exists above
   ❌ Explanatory text outside the tags

4. **AVAILABLE CLASSES TO EXTEND**:
   - ResponseValidator: Add validation methods
   - RequestLogger: Add logging methods
   - PerformanceMonitor: Add monitoring methods

### EXAMPLE OUTPUT:

<new_methods>
# For ResponseValidator:
    def validate_pagination(self, response, expected_page_size=20):
        """Validate paginated response structure."""
        data = response.json()
        assert "items" in data, "Missing items array"
        assert "total" in data, "Missing total count"
        assert len(data["items"]) <= expected_page_size
        return True

    def validate_error_response(self, response, expected_code):
        """Validate error response format."""
        data = response.json()
        assert "error" in data or "message" in data
        return response.status_code == expected_code

# For PerformanceMonitor:
    def record_response_size(self, endpoint, size_bytes):
        """Record response payload size for analysis."""
        if endpoint not in self.response_sizes:
            self.response_sizes[endpoint] = []
        self.response_sizes[endpoint].append(size_bytes)
</new_methods>

---

Now generate new methods based on the validation patterns above.
Return ONLY the new methods in <new_methods> tags. Nothing else.
