TASK: Generate EDGE CASE and BOUNDARY test scenarios for a SINGLE endpoint.

ENDPOINT TO TEST:
{{ endpoint }}

{% if auth_endpoints %}
AUTHENTICATION ENDPOINTS:
{{ auth_endpoints }}
{% endif %}

BASE WORKFLOW (inherit from this):
{{ base_workflow }}

TEST DATA AVAILABLE:
{{ test_data_content }}

REQUIREMENTS:

1. **EDGE CASE SCENARIOS** (Boundary Testing):
   Generate 3-5 @task methods for boundary conditions:
   - Empty strings in optional fields (weight: 2)
   - Zero values in numeric fields (weight: 2)
   - Maximum/minimum valid values (weight: 2)
   - Unicode/special characters (weight: 2)

2. **CLASS STRUCTURE**:
   ```python
   from base_workflow import BaseWorkflow
   import test_data

   class {{ class_name }}EdgeWorkflow(BaseWorkflow):
       """Edge case tests for {{ method }} {{ path }}"""

       @task(2)
       def test_empty_optional_string(self):
           """Test with empty string in optional field"""
           data = test_data.get_valid_data()
           data["optional_field"] = ""
           with self.client.{{ method|lower }}("{{ path }}", json=data, catch_response=True) as response:
               if response.status_code in [200, 201, 400, 422]:
                   response.success()
               else:
                   response.failure(f"Unexpected: {response.status_code}")
   ```

3. **IMPORTANT**:
   - Generate tests ONLY for this endpoint: {{ method }} {{ path }}
   - Keep code concise (50-80 lines max)
   - Edge cases may succeed or fail - both are valid
   - Do NOT include security tests

OUTPUT:
Return ONLY valid Python code in <code></code> tags.

<code>your_complete_python_code_here</code>
