TASK: Generate STATE-DEPENDENT test scenarios for a SINGLE endpoint.

ENDPOINT TO TEST:
{{ endpoint }}

{% if auth_endpoints %}
AUTHENTICATION ENDPOINTS:
{{ auth_endpoints }}
{% endif %}

BASE WORKFLOW (inherit from this):
{{ base_workflow }}

TEST DATA AVAILABLE:
{{ test_data_content }}

REQUIREMENTS:

1. **STATE-DEPENDENT SCENARIOS** (Resource Lifecycle):
   Generate 2-4 @task methods for state interactions:
   - Create then read/update/delete (weight: 5)
   - Duplicate creation - expect 409 (weight: 3)
   - Operation on non-existent resource (weight: 3)

2. **CLASS STRUCTURE**:
   ```python
   from base_workflow import BaseWorkflow
   import test_data

   class {{ class_name }}StateWorkflow(BaseWorkflow):
       """State-dependent tests for {{ method }} {{ path }}"""

       created_ids = []

       def on_start(self):
           super().on_start()
           self.created_ids = []

       @task(5)
       def test_create_then_read(self):
           """Test create followed by read"""
           # Create resource first, then perform operation
           ...
   ```

3. **IMPORTANT**:
   - Generate tests ONLY for this endpoint: {{ method }} {{ path }}
   - Keep code concise (50-80 lines max)
   - Focus on operations that depend on prior state
   - Clean up resources in on_stop() if needed

OUTPUT:
Return ONLY valid Python code in <code></code> tags.

<code>your_complete_python_code_here</code>
