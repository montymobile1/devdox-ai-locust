TASK: Generate POSITIVE and STATE-DEPENDENT test scenarios for a SINGLE endpoint.

ENDPOINT TO TEST:
{{ endpoint }}

{% if auth_endpoints %}
AUTHENTICATION ENDPOINTS:
{{ auth_endpoints }}
{% endif %}

BASE WORKFLOW (inherit from this):
{{ base_workflow }}

TEST DATA AVAILABLE:
{{ test_data_content }}

REQUIREMENTS:

1. **POSITIVE TEST SCENARIOS** (Happy Path):
   Generate 2-3 @task methods for successful operations:
   - Valid data with all required fields (weight: 10)
   - Valid data with optional fields included (weight: 8)

2. **STATE-DEPENDENT SCENARIOS**:
   Generate 1-2 @task methods for state interactions:
   - Create then read/update (weight: 5)
   - Operation on created resource (weight: 5)

3. **CLASS STRUCTURE**:
   ```python
   from locust import task
   from base_workflow import BaseWorkflow
   import test_data

   class {{ class_name }}PositiveWorkflow(BaseWorkflow):
       """Positive and state tests for {{ method }} {{ path }}"""

       created_ids = []

       @task(10)
       def test_success_with_valid_data(self):
           """Test successful operation"""
           with self.client.{{ method|lower }}("{{ path }}", ..., catch_response=True) as response:
               if response.status_code in [200, 201]:
                   response.success()
               else:
                   response.failure(f"Expected success, got {response.status_code}")
   ```

4. **IMPORTANT**:
   - Generate tests ONLY for this endpoint: {{ method }} {{ path }}
   - Keep code concise (60-100 lines max)
   - Use catch_response=True for proper Locust reporting

OUTPUT:
Return ONLY valid Python code in <code></code> tags.

<code>your_complete_python_code_here</code>
