TASK: Generate POSITIVE (happy path) test scenarios for a SINGLE endpoint.

ENDPOINT TO TEST:
{{ endpoint }}

{% if auth_endpoints %}
AUTHENTICATION ENDPOINTS:
{{ auth_endpoints }}
{% endif %}

BASE WORKFLOW (inherit from this):
{{ base_workflow }}

TEST DATA AVAILABLE:
{{ test_data_content }}

REQUIREMENTS:

1. **POSITIVE TEST SCENARIOS** (Happy Path Only):
   Generate 3-5 @task methods that test successful operations:
   - Valid data with all required fields (weight: 10)
   - Valid data with optional fields included (weight: 8)
   - Different valid data combinations (weight: 5)

2. **CLASS STRUCTURE**:
   ```python
   from base_workflow import BaseWorkflow
   import test_data

   class {{ class_name }}PositiveWorkflow(BaseWorkflow):
       """Positive tests for {{ method }} {{ path }}"""

       @task(10)
       def test_success_with_required_fields(self):
           """Test successful operation with required fields only"""
           # Use actual endpoint path and method
           with self.client.{{ method|lower }}("{{ path }}", ..., catch_response=True) as response:
               if response.status_code in [200, 201]:
                   response.success()
               else:
                   response.failure(f"Expected success, got {response.status_code}")
   ```

3. **IMPORTANT**:
   - Generate tests ONLY for this endpoint: {{ method }} {{ path }}
   - Keep code concise (50-80 lines max)
   - Use catch_response=True for proper Locust reporting
   - Do NOT include error handling or edge cases

OUTPUT:
Return ONLY valid Python code in <code></code> tags.

<code>your_complete_python_code_here</code>
