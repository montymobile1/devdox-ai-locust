TASK: Generate POSITIVE (happy path) test scenarios for the Locust workflow.

ENDPOINTS TO TEST:
{{ grouped_endpoints }}

{% if auth_endpoints %}
AUTHENTICATION ENDPOINTS:
{{ auth_endpoints }}
{% endif %}

BASE WORKFLOW (inherit from this):
{{ base_workflow }}

TEST DATA AVAILABLE:
{{ test_data_content }}

REQUIREMENTS:

1. **POSITIVE TEST SCENARIOS** (Happy Path Only):
   Generate @task methods that test successful API operations:
   - Valid data with all required fields (weight: 10)
   - Valid data with optional fields included (weight: 8)
   - Different valid data combinations (weight: 5)
   - Various valid input formats accepted by the API (weight: 5)

2. **CLASS STRUCTURE**:
   ```python
   from base_workflow import BaseWorkflow
   import test_data

   class {{ class_name }}PositiveWorkflow(BaseWorkflow):
       """Positive (happy path) tests for {{ tag_name }} endpoints"""

       def on_start(self):
           super().on_start()
           # Setup authentication if needed

       @task(10)
       def test_endpoint_success(self):
           """Test successful operation with valid data"""
           data = test_data.get_valid_data()
           with self.client.post("/path", json=data, catch_response=True) as response:
               if response.status_code in [200, 201]:
                   response.success()
               else:
                   response.failure(f"Expected 200/201, got {response.status_code}")
   ```

3. **VALIDATION**:
   - Check expected success status codes (200, 201, 204)
   - Validate response structure when applicable
   - Use catch_response=True for proper Locust reporting

4. **FOCUS**:
   - ONLY generate happy path tests
   - Do NOT include error handling, edge cases, or state-dependent tests
   - Keep tests simple and focused on successful operations

OUTPUT:
Return ONLY valid Python code in <code></code> tags.
Generate ONE class with positive test methods only.

<code>your_complete_python_code_here</code>
