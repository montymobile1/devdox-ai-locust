TASK: Generate SECURITY test scenarios for a single API endpoint.

=== CONTEXT ===

ENDPOINT TO TEST:
{{ endpoint }}

{% if auth_endpoints %}
AUTHENTICATION ENDPOINTS:
{{ auth_endpoints }}
{% endif %}

BASE WORKFLOW (inherit from this):
{{ base_workflow }}

TEST DATA MODULE (test_data.py):
{{ test_data_content }}

=== ALLOWED IMPORTS (use ONLY these) ===
```python
from locust import task
from workflows.base_workflow import BaseWorkflow
from test_data import test_data_generator
import random
```

=== CRITICAL: OUTPUT REQUIREMENTS ===

**ASCII ONLY**: Your output MUST contain ONLY ASCII characters (codes 0-127).
- NO Unicode characters (Chinese, Arabic, emoji, etc.)
- NO special characters outside standard ASCII
- NO non-English text anywhere in the code
- Variable names: a-z, A-Z, 0-9, underscore ONLY
- Strings: Use only ASCII printable characters
- Use RAW STRINGS for regex patterns: r"pattern" not "pattern"

If you output ANY non-ASCII character, the code will FAIL to parse.

=== SECURITY PAYLOADS ===
```python
SQL_PAYLOADS = ["' OR '1'='1", "'; DROP TABLE users; --", "1; SELECT * FROM users"]
XSS_PAYLOADS = ["<script>alert('XSS')</script>", "<img src=x onerror=alert(1)>"]
PATH_TRAVERSAL = ["../../../etc/passwd", "..\\..\\..\\windows\\system32"]
```

=== CHAIN OF THOUGHT INSTRUCTIONS ===

Before writing code, you MUST reason through these steps:

**STEP 1 - ANALYZE THE ENDPOINT:**
- What is the HTTP method and path?
- **CRITICAL**: Is this a RESPONSE-TESTING endpoint (path contains /responses/)?
  - Paths like /responses/404, /responses/500, /responses/206 ALWAYS return that status code
  - This is BY DESIGN - they exist to test how clients handle different HTTP responses
  - If YES: Skip security injection tests - these endpoints don't process input, they just return fixed responses
- Does it accept a request body? (POST/PUT/PATCH usually do)
- Does it have path parameters?

**STEP 2 - IDENTIFY INJECTION POINTS:**
- List ALL string fields from the schema (exact names!)
- These are where we inject payloads
- Path parameters can also be injection points

**STEP 3 - PLAN SECURITY TESTS:**
For each string field, plan:
- SQL injection test: inject SQL_PAYLOAD into that field
- XSS test: inject XSS_PAYLOAD into that field
- Include ALL required fields with valid data, only inject into ONE field at a time

**STEP 4 - DETERMINE EXPECTED BEHAVIOR:**
- 400/422: Properly rejected (good - input validation working)
- 500: POTENTIAL VULNERABILITY (bad - server error from injection)
- 200/201: May or may not be vulnerable (depends on if payload was sanitized)

=== CRITICAL: RESPONSE VALIDATION PATTERN ===

Security tests MUST validate server responses correctly.
Use this pattern:

```python
@task(3)
def test_sql_injection(self):
    payload = random.choice(SQL_PAYLOADS)
    data = {"name": payload, "other_field": "valid"}
    with self.client.post("/endpoint", json=data, catch_response=True) as response:
        if response.status_code in [400, 422]:
            response.success()  # Server properly rejected injection
        elif response.status_code == 500:
            response.failure("POTENTIAL VULNERABILITY: Server error from injection")
        else:
            response.success()  # 200/201 - payload was likely sanitized
```

**DO NOT** unconditionally call response.failure() - that marks the test as failed even when the server handles the injection correctly!

=== CONSTRAINTS ===
- ALL payloads must be SHORT: max 100 characters
- Keep total code under 80 lines
- Use test_data_generator for valid values in non-injected fields
- BYTES LITERALS MUST BE ASCII ONLY
- OUTPUT ASCII ONLY: No Unicode, no Chinese characters, no emoji
- Use RAW STRINGS for regex patterns: r"^pattern$" not "^pattern$"

=== OUTPUT FORMAT ===

First, show your analysis in <analysis> tags.
Then, provide ONLY valid Python code in <code> tags.
No markdown fences, no explanations outside the tags.

<analysis>
STEP 1 - ENDPOINT ANALYSIS:
Method: [method]
Path: [path]
Is response-testing endpoint: [yes/no - if path contains /responses/, skip security tests]
Accepts body: [yes/no]
Path params: [list them]

STEP 2 - INJECTION POINTS:
String fields from schema: [list exact field names]
Path params that accept strings: [list them]

STEP 3 - SECURITY TEST PLAN:
- SQL injection in field: [field name]
- XSS injection in field: [field name]
- Path traversal test: [if applicable]

STEP 4 - EXPECTED RESPONSES:
400/422 = Success (proper rejection)
500 = Failure (potential vulnerability)
</analysis>

<code>
from locust import task
from workflows.base_workflow import BaseWorkflow
from test_data import test_data_generator
import random

SQL_PAYLOADS = ["' OR '1'='1", "'; DROP TABLE users; --"]
XSS_PAYLOADS = ["<script>alert('XSS')</script>"]

class {{ class_name }}SecurityWorkflow(BaseWorkflow):
    """Security tests for {{ method }} {{ path }}"""

    @task(3)
    def test_sql_injection(self):
        # Your implementation based on analysis...
        pass
</code>
