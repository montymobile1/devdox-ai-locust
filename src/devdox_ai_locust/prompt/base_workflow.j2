You are enhancing the base workflow file for Locust load testing.

## CRITICAL RULES

1. **PRESERVE BaseTaskMethods class** - do NOT remove or replace it
2. **catch_response=True REQUIRES with-block**
3. **ONLY use endpoints from auth_endpoints** - do NOT invent URLs

{% if auth_endpoints %}
## Authentication Endpoints Available

{{ auth_endpoints }}

Add login_and_get_token() and logout_session() methods using these EXACT paths.
{% else %}
## No Authentication Endpoints

auth_endpoints is empty - do NOT create login/logout methods.
Use config.api_key for authorization if needed.
{% endif %}

## Current Base Workflow

```python
{{ base_content }}
```

## Available Endpoints

{{ grouped_enpoints }}

## Available Test Data

{{ test_data_content }}

{% if db_type == "mongo" %}
## MongoDB Integration

MongoDB is available. Add these methods to BaseTaskMethods:
- get_mongodb_test_data(collection, filter)
- cleanup_mongodb_test_data()
{% endif %}

---

## YOUR TASK

Enhance the base workflow while **PRESERVING** all existing classes and methods:

1. **Keep BaseTaskMethods** - only ADD new methods
2. **Add authentication** only if auth_endpoints has endpoints
3. **Add error handling** to existing methods
4. **Add data storage** for IDs between tasks

### OUTPUT FORMAT

```
<code>
# Complete enhanced base_workflow.py
...
</code>
```

### DO NOT:
- ❌ Remove BaseTaskMethods class
- ❌ Invent endpoints not in auth_endpoints
- ❌ Use catch_response without with-block
- ❌ Add explanatory text outside <code> tags

Return the complete enhanced file now.
