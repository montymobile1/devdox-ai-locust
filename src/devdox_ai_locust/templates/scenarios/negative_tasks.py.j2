"""Negative test scenarios - tasks expecting errors."""
import logging
from locust import task

from .base_scenario import BaseScenario

logger = logging.getLogger(__name__)


class NegativeTasks(BaseScenario):
    """
    Negative test scenarios with invalid data expecting errors.

    Weight distribution: ~25% of total load
    """

    # AI-generated tasks below
{{ ai_generated_tasks | indent(4, first=True) }}
